[tool.uv.workspace]
members = ["core", "server"]

[tool.uv]
package = false

[tool.pyright]
extraPaths = ["core/src", "server/src"]

[dependency-groups]
dev = [
    "pytest>=8.3.0",
]

[tool.pytest.ini_options]
testpaths = ["core/tests"]
